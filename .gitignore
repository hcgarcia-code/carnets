# Entorno virtual
venv/
env/
.venv/

# Variables de entorno y secretos
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/

# Base de datos local (contiene datos personales de afiliados)
db.sqlite3
*.sqlite3

# Archivos estáticos generados (collectstatic)
staticfiles/

# Editor / SO
.vscode/
.idea/
.DS_Store

# Configuración local de Claude Code (específica de esta máquina)
.claude/

# Skills instaladas por la CLI de skills: 4.500 archivos de terceros que no
# mantenemos. Un `git add -A` distraído los metería en un repositorio que
# trata datos de categoría especial. Se reinstalan con `npx skills`.
.agents/
skills-lock.json

# Excel de ejemplo generado por `manage.py preparar_pruebas`
afiliados_de_ejemplo.xlsx

# Lienzo de diseño ya montado (~2 MB de editor empaquetado). Se regenera desde
# los .dc.html de diseno/, que sí van al repositorio.
diseno/portada-carnets-cgt.html
